<h1>{{restaurant.name}}</h1>
<br>
    <article class="menus">
        {{#each restaurant.menus}}
            <main>{{this.title}}</main>
            <br>
            {{#each this.items}}
                <p>{{this.name}}</p>
            {{/each}}
            <br>
        {{/each}}
    </article>


<script src="../about.js"></script>