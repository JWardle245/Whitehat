import javax.servlet.http.HttpSession;

public final class SessionHelper {

    public static SystemInfo getSystemInfo(HttpSession session, int id) {
        SystemInfo systemInfo = new SystemInfo();
        return systemInfo;
    }
}
